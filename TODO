Also we need to  change max_steps on max_steps_without_food, and adjust logic
What is the purpose of target model?
How can i put main 2 versions with graphics and without together?


WARNING:absl:The `save_format` argument is deprecated in Keras 3. We recommend removing this argument as it can be inferred from the file path. Received: save_format=keras

add timestamp to saved models.

Why output should be linear for Q values? Why not softmax?


Later i will multithread learning, but not now.
